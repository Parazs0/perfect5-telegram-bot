services:
  - type: worker
    name: perfect5-signal-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python perfect5_signal_bot.py"
    autoDeploy: true
